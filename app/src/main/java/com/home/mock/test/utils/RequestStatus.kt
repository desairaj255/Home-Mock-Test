package com.home.mock.test.utils

// Enum class representing the status of a request
enum class RequestStatus {
    LOADING, // Represents that the request is currently loading or in progress
    ERROR,   // Represents that the request encountered an error
    DONE     // Represents that the request has been completed successfully
}
